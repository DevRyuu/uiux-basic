/**
 * 변수명 규칙:
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용 할 수 있다.
 * 2) 특수기호는 언더스코어와 달러 기호만 사용 가능하다.
 * 3) 숫자는 변수명의 첫 글자로 사용할 수 없다.
 * 4) 예약어는 변수명으로 사용할 수 없다.
 * 
 */

const codeFactory = '코드팩토리';
const $ive = '아이브';
const _yuJin = '안유진';

console.log(codeFactory);
console.log($ive);
console.log(_yuJin);

/**
 * 변수명 스타일:
 * 1) camelCase - 대부분의 언어에서 많이 사용
 * 2) snake_case - Python에서 많이 사용
 * 3) PascalCase - 클래스명을 선언할 때 사용
 */

const camelCase = 'camelCase';
const snake_case = 'snake_case';
const PascalCase = 'PascalCase';

console.log(camelCase);
console.log(snake_case);
console.log(PascalCase);